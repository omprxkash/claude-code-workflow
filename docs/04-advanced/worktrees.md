# Git worktrees

> **Stub.** Outline below; full write-up to come.

Git worktrees let you check out multiple branches into separate folders at once — the
key to running several Claude Code sessions in parallel without them stepping on each
other.

## To cover

- What a worktree is and how it differs from switching branches.
- Why it pairs with Claude Code: one isolated working copy per parallel session.
- Spinning up parallel sessions safely; avoiding the conflicts/headaches of multiple
  agents in one directory.
- Session mobility — moving work between machines/surfaces.
- Cleanup and merging the parallel work back together.
- Cross-link: [parallelization](parallelization.md).

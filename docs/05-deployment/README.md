# 05 — Deployment

Running your automations without Claude Code present. The key distinction: self-healing (Claude fixes itself) vs deterministic (Python scripts that run the same way every time).

| File | What it covers |
|---|---|
| [deploy.md](deploy.md) | Modal, trigger.dev, Vercel, GitHub Actions — when to use each; self-healing vs deterministic |
